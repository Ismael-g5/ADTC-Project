<h2>Sobre Nós</h2>
  <div class="row content-ab">
 
  {% for item in content %}
   <div class="col-md-3">
    <a href="/node/{{ item.nid }}">{{ item.title }}</a>
    <div class="content-description">
    <img style="width: 50px; height: 50px;" src="{{ item.image_url }}" alt="{{ item.title }}">

  </div>
  </div>
{% endfor %}

</div>
 
   
  